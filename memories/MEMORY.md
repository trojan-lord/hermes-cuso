GPU: niri display session renders on AMD Renoir iGPU (RADV); GTX 1650 Ti compute-only (Demucs htdemucs). gamescope 3.16 installed.
§
mumble_monster = 'papa' to Cuso. Pola' Bea' = 'Daddy' / 'Dady big balls'. Two different people, different Discord IDs. Daddy is playful/teasing — tests boundaries, laughs when refused. Dynamic is lighthearted.
§
GitHub: trojan-lord. gh auth active. Push only existing repos; ask first. .hermes syncs via cron hermes-backup-sync (sync-hermes-backup.sh clones fresh to /tmp → trojan-lord/hermes-cuso); never push from local clones. SOUL.md ref rule: no verbatim show quotes, only flavor/patterns.
§
TTS: marshall provider = Qwen Talker 1.7B, ICL one-shot cloning (clip_07_trimmed_precise); script ~/Projects/marshall-voice/tts-provider/tts-provider.sh. MUST skill_view('marshall-speech-patterns') before TTS. Curl Discord API (never MEDIA: tag); token DISCORD_BOT_TOKEN; voice channel 1526410959772717106.
§
Gateway restart: script via tuistory. Load skills, stay in character, verify from source first.
§
User tests on iPad via Discord. Serve HTML via HTTP on port 8080 (UFW open). Bundle p5.js inline. Cloudflare tunnel blocked. Ngrok needs auth.
§
§ User enjoys storytelling & character voice memos (spooky stories, CSE characters). Speech pattern feedback: don't overcorrect with rigid rules — "tone down strictness, every now and then it's fine." Balance > rigidity.
§
User preference: after 2 consecutive failed approaches on a task, stop and report what was tried and what's needed — don't silently try more.
§
gh token lapses; recover PAT from ~/.git-credentials: awk -F'[/:@]' '{print $5}' ~/.git-credentials | gh auth login --with-token
§
Kanban: use for multi-step/long-running/dependency jobs. Board ~/.hermes/kanban.db 'default'; only 'default' profile exists — workers spawn as default-profile. Flag qualifying jobs instead of inline.
§
TTS comparisons: same-text head-to-head before claiming better than Qwen ICL (user caught extrapolation).
§
opencode-zen: API key can be literal "public" (no signup). Free models: deepseek-v4-flash-free, mimo-v2.5-free, hy3-free, nemotron-3-ultra-free, north-mini-code-free.